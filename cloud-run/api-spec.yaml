swagger: "2.0"
info:
  title: AICC API
  description:
  version: 1.0.0

host: my-app-image-174827312206.us-central1.run.app
basePath: /
schemes:
  - https

paths:
  /signup:
    post:
      summary: adds the user to firestore collection using uid generated by firebase auth in flutter app
      description:
      consumes:
        - application/json
      produces:
        - application/json
      responses:
        200:
          description: OK

  /login:
    post:
      summary: receives firebase id_token as a form of 'password' to verify user.
      description:
      consumes:
        - application/json
      produces:
        - application/json
      responses:
        200:
          description: OK